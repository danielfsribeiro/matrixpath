*** FILES ***
test/tree.csv
	CSV file (tab separated) containing 2 columns:
	Node id in Col 1 and its respective Parent id in Col 2.
	Col 1: Node id (int)
	Col 2: Parent id (int). If root, Parent id == Node id 
	
tree.jpeg
	Example tree used in tests.
matrix.jpeg
	Example matrix used in tests derived from tree.jpg.
	
